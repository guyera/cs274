# CS 162 Lecture Notes

This is a static site built via NextJS SSG.

`npm run dev`
	Starts the development server locally on port `51627`.

`npm run build` builds the app for production. Exports to static site in `out/`.
